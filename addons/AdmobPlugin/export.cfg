; Read by AdmobExportConfig.gd at iOS/Android export time. Drives the
; GADApplicationIdentifier / android APPLICATION_ID, the ATT prompt string, and
; (implicitly) which app id the SDK boots with.
;
; is_real=false  -> uses the [Debug] app id = Google's test app -> TEST ADS.
; For the real App Store launch: set is_real=true here AND flip
; Config.ADS_FORCE_TEST to false in game/config.gd (both must agree).

[General]

is_real=false

[Debug]

app_id="ca-app-pub-3940256099942544~1458002511"

[Release]

app_id="ca-app-pub-5040304268747359~5303167211"

[ATT]

att_enabled=true
att_text="Cottage Sort uses your device identifier to show ads that are more relevant to you."
